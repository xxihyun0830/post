extends layout

//- 로그인 창
block content
  .content
    .clearfix
      form.form-signin(action='/signin', method='POST')
        h3.form-signin-heading Log-In 

        label.sr-only(for='email') Email-address
        input(
            type = 'email', 
            id = 'email',
            name = 'email',
            class = 'form-control',
            placeholder='메일주소 입력',
            required,
            autofocus
        )
        label.sr-only(for='password') Password
        input(
            type = 'password',
            id = 'password',
            name = 'password',
            class = 'form-control',
            placeholder = '비밀번호 입력',
            required,
            autofocus
        )
        button.btn.btn-primary.btn-block(type='submit') 로그인하기       
        div.sub
          a(href='users/new') 회원가입 신청하기 