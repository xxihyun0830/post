.form-group
  label(for="title") Title
  input.form-control(
      type="text", 
      name = "title",
      placeholder = "댓글 달기 ...",
      value = post.title)

.form-group
  label(for="post") Post
  textarea.form-control(name = "content",row = "5") = post.content

